# Experiment: Qwen3-8B + recursive 1024-char chunking + 300 docs
base_config: ../../configs/base.yaml

experiment_id: qwen_recursive_1024_300
description: "Qwen3-8B embedder, recursive 1024-char chunks, 300-doc corpus"

dataset:
  max_docs: 300

chunker:
  type: chunkers.recursive.RecursiveChunker
  max_chunk_chars: 1024
  overlap_chars: 128

embedder:
  type: embedders.qwen.QwenEmbedder
  model_name: Qwen/Qwen3-Embedding-8B
  device: cuda:0
  batch_size: 16

evaluation:
  k_values: [3, 5, 10]
  metrics:
    - recall_at_k
    - doc_recall_at_k
    - hit_at_k
    - mrr

indexing:
  document_batch_size: 10
  embedding_batch_size: 16
  save_intermediate_every: 50

logging:
  level: DEBUG
