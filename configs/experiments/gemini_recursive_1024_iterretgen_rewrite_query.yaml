# Experiment: IterRetGen with Gemini embedding + recursive 1024-char chunks + query rewriting
base_config: ../../configs/base.yaml

experiment_id: gemini_recursive_1024_iterretgen_rewrite_query_1k-docs
description: "IterRetGen: Gemini embedder, recursive 1024-char chunks, Gemini query rewriting"

dataset:
  max_docs: 1000

chunker:
  type: chunkers.recursive.RecursiveChunker
  max_chunk_chars: 1024
  overlap_chars: 128

embedder:
  type: embedders.gemini.GeminiEmbedder
  model_name: gemini-embedding-001
  task_type: RETRIEVAL_DOCUMENT
  batch_size: 50
  max_cost_usd: 10.0

query_rewriter:
  type: rewriters.gemini_rewriter.GeminiQueryRewriter
  model_name: gemini-2.5-flash

generator:
  type: generators.gemini.GeminiGenerator
  model_name: gemini-2.5-flash
  max_output_tokens: 1024

iterretgen:
  max_iterations: 3
  short_answer: false
  intermediate_model_name: gemini-2.5-flash
  intermediate_max_output_tokens: 1024

evaluation:
  k_values: [3, 5, 10, 20, 50]
  metrics:
    - recall_at_k
    - doc_recall_at_k
    - hit_at_k
    - mrr
    - ndcg_at_k

indexing:
  document_batch_size: 10
  embedding_batch_size: 50
  save_intermediate_every: 50

logging:
  level: DEBUG
